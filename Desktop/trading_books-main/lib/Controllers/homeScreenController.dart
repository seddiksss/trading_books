import 'package:get/get.dart';

class HomeScreenController extends GetxController {
  // int currentPag = 0;
  // List<Widget> pageList = [const Home(), const Locations(), const Settings()];
  // changePage(int index) {
  //   currentPag = index;
  //   update();
  // }
}
